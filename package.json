{
  "name": "sample-stepfunctions-agentcore-coding-agent",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "app": "bin/app.ts"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "seed": "tsx scripts/seed.ts"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.162",
    "@types/jest": "^30.0.0",
    "@types/node": "^26.1.1",
    "aws-cdk": "^2.1131.0",
    "esbuild": "^0.28.1",
    "jest": "^30.4.2",
    "ts-jest": "^29.4.11",
    "tsx": "^4.23.1",
    "typescript": "~6.0.3"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.1092.0",
    "@aws-sdk/client-cloudformation": "^3.1088.0",
    "@aws-sdk/client-rds-data": "^3.1092.0",
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.7.0",
    "csv-parse": "^5.6.0",
    "source-map-support": "^0.5.21",
    "yaml": "^2.9.0"
  },
  "//overrides": [
    "brace-expansion <=5.0.7 carries two DoS advisories (GHSA-3jxr-9vmj-r5cp,",
    "GHSA-mh99-v99m-4gvg) and was pulled in transitively by both aws-cdk-lib and",
    "the jest toolchain, producing 20 high-severity findings on npm install.",
    "Pinning the patched 5.0.8 clears 19 of 20 without touching the toolchain.",
    "Do NOT run `npm audit fix`: it proposes DOWNGRADING jest to 25.x, which is",
    "worse than the advisory it resolves.",
    "",
    "The 1 remaining finding is aws-cdk-lib's own VENDORED copy at",
    "node_modules/aws-cdk-lib/node_modules/brace-expansion. aws-cdk-lib sets",
    "bundleDependencies, so npm `overrides` cannot reach inside it.",
    "",
    "Verified 2026-07-28, not assumed:",
    "  - aws-cdk-lib@2.262.1 is the LATEST published version, and it still",
    "    vendors brace-expansion 5.0.7 - so there is no newer release to",
    "    upgrade to. This must be fixed upstream by AWS.",
    "  - The vendored copy is build-time only: it appears in no synthesized",
    "    Lambda bundle under cdk.out/asset.*/index.js, so it is not deployed",
    "    and is not reachable at runtime.",
    "  - Nothing in this repo expands untrusted brace patterns.",
    "Re-check on each aws-cdk-lib bump; drop this note once upstream ships",
    ">=5.0.8. Tracked in docs/KNOWN-ISSUES.md."
  ],
  "overrides": {
    "brace-expansion": "^5.0.12"
  }
}
