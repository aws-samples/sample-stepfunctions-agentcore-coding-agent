/**
 * First state in the coding workflow. Placeholder for a deterministic direct
 * lookup - always returns an empty string for now, so the Choice state that
 * follows always falls through to the CodingAgent. Accepts no meaningful
 * input; real lookup parameters are still being decided.
 */
export const handler = async () => {
  return '';
};
